// services/inventory-cpp/main.cpp
#include <iostream>

// We will add a web framework and Redis client later.
// For now, this is just a placeholder to ensure our Docker setup works.
int main() {
    std::cout << "Inventory service starting..." << std::endl;
    // Keep the service running
    while(true) {}
    return 0;
}